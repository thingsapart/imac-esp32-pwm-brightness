target 'imac-esp32-pwm-brightness' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'MediaKeyTap', :git => 'https://github.com/the0neyouseek/MediaKeyTap.git', :branch => 'master'
  pod "ORSSerialPort"
end
